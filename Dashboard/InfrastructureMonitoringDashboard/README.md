# Dashboard Layer

This folder contains a sanitized .NET dashboard example for the monitoring platform.

Included components:

- minimal ASP.NET Core backend with JSON API endpoints for monitoring data
- static frontend assets for the operator dashboard
- sample configuration file with placeholder connection values

The original internal implementation was generalized for public sharing. Internal organization names, domains, credentials, server names, and localized text were removed or translated.
